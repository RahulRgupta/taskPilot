<div class="row mb-3">
  <% if ( typeof inputLabel !== 'undefined' && inputLabel ) { %>
    <label class="col-sm-3 col-form-label"><%= inputLabel %></label>
  <% } %>

  <div class="col-sm-9">
    <textarea 
      id="<%= inputId %>"
      name="<%= inputId %>"
      rows="5"
      class="form-control <%= isEditor ? 'editor' : '' %>"><%= value %></textarea>

    <label for="<%= inputId %>" class="invalid-feedback"></label>  
  </div>
</div>