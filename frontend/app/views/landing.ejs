<div class="landing-page">
  <!-- Header / Nav -->
  <header class="landing-header">
    <div class="landing-nav">
      <a href="/" class="landing-logo">
        <img src="/assets/img/logos/task-pilot-icon.svg" alt="Task Pilot" class="landing-logo-img">
        <span class="landing-logo-text">Task Pilot</span>
      </a>
      <div class="landing-nav-btns">
        <a href="/register" class="landing-btn-register">Register</a>
        <a href="/login" class="landing-btn-signin">Sign In</a>
      </div>
    </div>
  </header>

  <!-- Hero -->
  <section class="landing-hero">
    <div class="landing-hero-content">
      <h1 class="landing-hero-title">Navigate your work with clarity</h1>
      <p class="landing-hero-subtitle">
        Task Pilot is a multi-tenant task management system for organizations. 
        Steer projects to success—assign tasks, collaborate with your team, and never miss a deadline.
      </p>
      <div class="landing-hero-cta">
        <a href="/login" class="landing-btn-primary">
          Get Started
          <i class="ti ti-arrow-right"></i>
        </a>
      </div>
    </div>
    <div class="landing-hero-visual">
      <div class="landing-hero-card">
        <div class="landing-card-icon"><i class="ti ti-checklist"></i></div>
        <span>Projects</span>
      </div>
      <div class="landing-hero-card">
        <div class="landing-card-icon"><i class="ti ti-user-check"></i></div>
        <span>Teams</span>
      </div>
      <div class="landing-hero-card">
        <div class="landing-card-icon"><i class="ti ti-flag"></i></div>
        <span>Tasks</span>
      </div>
    </div>
  </section>

  <!-- Features -->
  <section class="landing-features">
    <h2 class="landing-section-title">Built for every role</h2>
    <p class="landing-section-subtitle">Role-based dashboards keep everyone focused on what matters.</p>
    <div class="landing-features-grid">
      <div class="landing-feature-card">
        <div class="landing-feature-icon"><i class="ti ti-building"></i></div>
        <h3>Admin</h3>
        <p>Create & manage organizations. Oversee all activity from one place.</p>
      </div>
      <div class="landing-feature-card">
        <div class="landing-feature-icon"><i class="ti ti-users"></i></div>
        <h3>Organization</h3>
        <p>Add managers & employees, create projects, and build your team.</p>
      </div>
      <div class="landing-feature-card">
        <div class="landing-feature-icon"><i class="ti ti-user-star"></i></div>
        <h3>Manager</h3>
        <p>Assign tasks, track progress, and keep projects on schedule.</p>
      </div>
      <div class="landing-feature-card">
        <div class="landing-feature-icon"><i class="ti ti-user"></i></div>
        <h3>Employee</h3>
        <p>See your tasks, update status, and stay in sync with your team.</p>
      </div>
    </div>
  </section>

  <!-- CTA -->
  <section class="landing-cta">
    <h2>Ready to pilot your tasks?</h2>
    <p>Sign in or create an account to get started.</p>
    <a href="/login" class="landing-btn-primary landing-btn-lg">
      Sign In
      <i class="ti ti-arrow-right"></i>
    </a>
  </section>

  <!-- Footer -->
  <footer class="landing-footer">
    <p>© <script>document.write(new Date().getFullYear());</script> Task Pilot. All rights reserved.</p>
  </footer>
</div>

<style>
.landing-page {
  min-height: 100vh;
  background: #f8f7fa;
}

.landing-header {
  padding: 1rem 2rem;
  position: fixed;
  top: 0;
  left: 0;
  right: 0;
  z-index: 100;
  background: rgba(255, 255, 255, 0.9);
  backdrop-filter: blur(10px);
  border-bottom: 1px solid rgba(226, 232, 240, 0.8);
}

.landing-nav {
  max-width: 1200px;
  margin: 0 auto;
  display: flex;
  align-items: center;
  justify-content: space-between;
}

.landing-logo {
  display: flex;
  align-items: center;
  gap: 0.75rem;
  text-decoration: none;
  color: #1e293b;
  font-weight: 700;
  font-size: 1.25rem;
}

.landing-logo-img {
  width: 40px;
  height: 40px;
  object-fit: contain;
}

.landing-nav-btns {
  display: flex;
  align-items: center;
  gap: 0.5rem;
}

.landing-btn-register {
  padding: 0.5rem 1rem;
  font-size: 0.95rem;
  font-weight: 600;
  color: #64748b;
  background: transparent;
  border: 2px solid #e2e8f0;
  border-radius: 10px;
  text-decoration: none;
  transition: all 0.2s;
}

.landing-btn-register:hover {
  color: #0d9488;
  border-color: #0d9488;
  background: #f8f7fa;
}

.landing-btn-signin {
  padding: 0.5rem 1.25rem;
  font-size: 0.95rem;
  font-weight: 600;
  color: #0d9488;
  background: transparent;
  border: 2px solid #0d9488;
  border-radius: 10px;
  text-decoration: none;
  transition: all 0.2s;
}

.landing-btn-signin:hover {
  background: #0d9488;
  color: #fff;
}

.landing-hero {
  padding: 8rem 2rem 6rem;
  max-width: 1200px;
  margin: 0 auto;
  display: flex;
  flex-direction: column;
  align-items: center;
  text-align: center;
}

.landing-hero-content {
  max-width: 680px;
}

.landing-hero-title {
  font-size: clamp(2rem, 5vw, 3rem);
  font-weight: 800;
  color: #1e293b;
  margin: 0 0 1rem 0;
  letter-spacing: -0.03em;
  line-height: 1.15;
}

.landing-hero-subtitle {
  font-size: 1.15rem;
  color: #64748b;
  margin: 0 0 2rem 0;
  line-height: 1.6;
}

.landing-hero-cta {
  display: flex;
  gap: 1rem;
  justify-content: center;
  flex-wrap: wrap;
}

.landing-btn-primary {
  display: inline-flex;
  align-items: center;
  gap: 0.5rem;
  padding: 0.875rem 1.75rem;
  font-size: 1rem;
  font-weight: 600;
  color: #fff;
  background: #0d9488;
  border: none;
  border-radius: 12px;
  text-decoration: none;
  transition: transform 0.2s, box-shadow 0.2s;
}

.landing-btn-primary:hover {
  transform: translateY(-2px);
  box-shadow: 0 8px 24px rgba(13, 148, 136, 0.4);
  background: #0f766e;
  color: #fff;
}

.landing-btn-secondary {
  display: inline-flex;
  align-items: center;
  padding: 0.875rem 1.75rem;
  font-size: 1rem;
  font-weight: 600;
  color: #64748b;
  background: #fff;
  border: 2px solid #e2e8f0;
  border-radius: 12px;
  text-decoration: none;
  transition: all 0.2s;
}

.landing-btn-secondary:hover {
  border-color: #0d9488;
  color: #0d9488;
}

.landing-hero-visual {
  display: flex;
  gap: 1rem;
  margin-top: 3rem;
  flex-wrap: wrap;
  justify-content: center;
}

.landing-hero-card {
  display: flex;
  flex-direction: column;
  align-items: center;
  gap: 0.5rem;
  padding: 1.5rem 2rem;
  background: #fff;
  border-radius: 16px;
  box-shadow: 0 4px 20px rgba(0, 0, 0, 0.08);
  font-weight: 600;
  color: #475569;
}

.landing-card-icon {
  width: 48px;
  height: 48px;
  display: flex;
  align-items: center;
  justify-content: center;
  background: #0d9488;
  border-radius: 12px;
  color: #fff;
  font-size: 1.5rem;
}

.landing-features {
  padding: 4rem 2rem;
  max-width: 1200px;
  margin: 0 auto;
}

.landing-section-title {
  font-size: 2rem;
  font-weight: 700;
  color: #1e293b;
  text-align: center;
  margin: 0 0 0.5rem 0;
}

.landing-section-subtitle {
  text-align: center;
  color: #64748b;
  margin: 0 0 3rem 0;
}

.landing-features-grid {
  display: grid;
  grid-template-columns: repeat(auto-fit, minmax(240px, 1fr));
  gap: 1.5rem;
}

.landing-feature-card {
  padding: 2rem;
  background: #fff;
  border-radius: 16px;
  box-shadow: 0 4px 20px rgba(0, 0, 0, 0.06);
  transition: transform 0.2s, box-shadow 0.2s;
}

.landing-feature-card:hover {
  transform: translateY(-4px);
  box-shadow: 0 12px 32px rgba(0, 0, 0, 0.1);
}

.landing-feature-icon {
  width: 48px;
  height: 48px;
  display: flex;
  align-items: center;
  justify-content: center;
  background: rgba(13, 148, 136, 0.15);
  border-radius: 12px;
  color: #0d9488;
  font-size: 1.5rem;
  margin-bottom: 1rem;
}

.landing-feature-card h3 {
  font-size: 1.15rem;
  font-weight: 600;
  color: #1e293b;
  margin: 0 0 0.5rem 0;
}

.landing-feature-card p {
  font-size: 0.95rem;
  color: #64748b;
  margin: 0;
  line-height: 1.5;
}

.landing-cta {
  padding: 5rem 2rem;
  text-align: center;
  background: #0d9488;
  color: #fff;
}

.landing-cta h2 {
  font-size: 1.75rem;
  font-weight: 700;
  margin: 0 0 0.5rem 0;
}

.landing-cta p {
  opacity: 0.9;
  margin: 0 0 1.5rem 0;
}

.landing-btn-lg {
  padding: 1rem 2rem;
  font-size: 1.1rem;
}

.landing-btn-lg:hover {
  background: rgba(255, 255, 255, 0.2);
  border: 2px solid #fff;
  color: #fff;
}

.landing-footer {
  padding: 1.5rem 2rem;
  text-align: center;
  color: #94a3b8;
  font-size: 0.9rem;
}

@media (max-width: 640px) {
  .landing-hero {
    padding-top: 6rem;
  }

  .landing-hero-cta {
    flex-direction: column;
  }

  .landing-btn-primary,
  .landing-btn-secondary {
    width: 100%;
    justify-content: center;
  }
}
</style>
